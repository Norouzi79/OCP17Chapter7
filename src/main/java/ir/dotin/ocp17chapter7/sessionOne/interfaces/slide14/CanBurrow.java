package ir.dotin.ocp17chapter7.sessionOne.interfaces.slide14;

/**
 * As you can see in your IDE implicit modifiers are grayed out by your IDE
 * implicit modifiers are added automatically by the JRE
 * so there's no need to write them yourself.
 */
public abstract interface CanBurrow {
    public abstract  Float getSpeed(int age);

    public static final int MINIMUM_DEPTH = 2;
}
